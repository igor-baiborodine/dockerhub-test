language: bash
services: docker

branches:
  only:
    - dev

env:
  - VERSION=7.3.4-ga7 VARIANT=jdk8-slim

install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - git clone https://github.com/docker-library/official-images.git ~/official-images

before_script:
  - env | sort
  - cd "$VERSION/$VARIANT"
  - image="$DOCKER_USERNAME/dockerhub-test:$VERSION-$VARIANT"

script:
  - |
    (
      set -Eeuo pipefail
      set -x
      docker build -t "$image" .
      ~/official-images/test/run.sh "$image"
      docker push "$image"
      ./generate-readme.sh "$VERSION-$VARIANT" "$TRAVIS_COMMIT"
      git status
    )

after_script:
  - docker images
